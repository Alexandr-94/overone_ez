1. Алфавит self.letters = list(letters_str) вы добавили в
родительский клас через наследующий
класс super().__init__('En', string.ascii_uppercase) ??


2. Непонятно по какому принцепу проходит проверка
(типо по регистры большому??)??
    def is_en_letter(self, letter):
        if letter.upper() in self.letters:
            return True
        return False


3. super().__init__('En', string.ascii_uppercase) тут получается вы
присвоили так сказать новые параметры переменным которые
инициализировали в родительском классе и
которые так же есть, и в наследуемом??

вы сделали то что описанно ниже это из лекции.
(мы его перегружаем. Иными словами, мы заменяем родительский
 метод__init__()собственным одноименным методом.
 Это чревато тем, что родительский метод просто в принципе
 не будет вызван, и мы потеряем его функционал в классе наследнике. )

